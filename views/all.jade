extends ./layout.jade
block contenido
    a(href="http://localhost:3001" target="_blank"  class="btn btn-success") Iniciar cliente Angular
    p
    p!= rjson['results'][0].name
    - for (var i = rjson['results'].length -1 ; i >= 0; i--)
     p!= rjson['results'][i].name


